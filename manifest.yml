name: netlify-trigger-action
inputs:
  - name: owner
    description: GitHub repository owner or organization name
    required: true
  - name: repo
    description: GitHub repository name
    required: true
  - name: workflow
    description: Filename of the workflow file
    required: true
  - name: branch
    description: Repo's branch to trigger the workflow on
